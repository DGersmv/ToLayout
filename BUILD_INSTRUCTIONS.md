# Инструкция по сборке проекта

## 🎯 Для Debug (с логами) — медленнее, но показывает всё

### Шаг 1: Откройте командную строку
Нажмите `Win + R`, введите `cmd`, нажмите Enter

### Шаг 2: Перейдите в папку проекта
```bash
cd "C:\Program Files\Graphisoft\API Development Kit 27.6003\Examples\BrowserReplInt"
```
**В офисе замените на ваш путь!**

### Шаг 3: Настройте проект (только если меняли CMakeLists.txt)
```bash
cmake -S . -B build
```

### Шаг 4: Соберите Debug-версию
```bash
cmake --build build --config Debug
```

### Шаг 5: Готово!
Файл будет здесь: `ToArchicad\Debug\Browser_Repl_Int.apx` (Debug) или `ToArchicad\Release\LandscapeHelper_AC27.apx` (Release)

---

## 🚀 Для Release (без логов, быстрая) — финальная версия

### Шаг 1: Откройте командную строку
Нажмите `Win + R`, введите `cmd`, нажмите Enter

### Шаг 2: Перейдите в папку проекта
```bash
cd "C:\Program Files\Graphisoft\API Development Kit 27.6003\Examples\BrowserReplInt"
```
**В офисе замените на ваш путь!**

### Шаг 3: Настройте проект (только если меняли CMakeLists.txt)
```bash
cmake -S . -B build
```

### Шаг 4: Соберите Release-версию
```bash
cmake --build build --config Release
```

### Шаг 5: Готово!
Файл будет здесь: `ToArchicad\Release\LandscapeHelper_AC27.apx`

---

## 📋 Разница между Debug и Release

| Функция | Debug | Release |
|---------|-------|---------|
| Логи в палитре | ✅ Включены, поле видимо | ❌ Выключены, поле скрыто |
| Логи в Archicad | ✅ Включены | ❌ Выключены |
| Скорость выполнения | 🐌 Медленно (~0.5-2 сек медленнее) | ⚡ Быстро |
| Оптимизация таблиц | ⚡ Быстро | ⚡ Быстро |
| Когда использовать | 🐛 Отладка, тестирование | 📦 Финальная версия |

**Оптимизации в обоих режимах:**
- ✅ Удалены избыточные логи в `UpdateSelectedElements()` (было 9, стало 0)
- ✅ Таблица обновляется одним `innerHTML` вместо множества `appendChild()`
- ✅ Добавлен буфер для логов (скорость + ограничение памяти)
- ✅ В Release чёрное поле логов скрывается через JS после загрузки HTML

---

## 🏢 Для офиса (другой диск/папка)

Если у вас другая структура папок, используйте:
```bash
cd "ВАШ_ПУТЬ\Browser_Repl_Int"
cmake -S . -B build -DAC_API_DEVKIT_DIR="ПОЛНЫЙ_ПУТЬ_К_DEVKIT"
cmake --build build --config Release
```

---

## 🔄 Быстрая пересборка (если ничего не меняли в CMakeLists.txt)

Просто:
```bash
# Debug
cmake --build build --config Debug

# Release
cmake --build build --config Release
```

---

## 💡 Установка в Archicad

1. Откройте Archicad
2. Удобства → Удобства (Add-ons)
3. Найдите ваш .apx файл
4. Нажмите на него → Replace/Install

